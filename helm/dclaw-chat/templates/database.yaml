{{- if .Values.database.enabled }}
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: {{ include "dclaw-chat.fullname" . }}-db
  labels:
    {{- include "dclaw-chat.labels" . | nindent 4 }}
spec:
  instances: {{ .Values.database.instances }}
  storage:
    size: {{ .Values.database.storage }}
  resources:
    {{- toYaml .Values.database.resources | nindent 4 }}
  bootstrap:
    initdb:
      database: dclaw_chat
      owner: postgres
  postgresql:
    parameters:
      max_connections: "100"
      shared_buffers: "256MB"
{{- end }}
