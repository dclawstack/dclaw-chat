apiVersion: v1
kind: Service
metadata:
  name: dclaw-chat
  namespace: dclaw-core
  labels:
    app.kubernetes.io/name: dclaw-chat
    app.kubernetes.io/part-of: dclaw-platform
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
      nodePort: 30002
  selector:
    app.kubernetes.io/name: dclaw-chat
