server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Next.js static export: routes are /<name>.html not /<name>/index.html
    location / {
        try_files $uri $uri.html $uri/ =404;
    }

    error_page 404 /404.html;
}
